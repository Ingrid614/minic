void main() {
    
    // Affectation simple
    int a = 1;
    int b = 3;

    // Affectation de variables
    int c = a;

    // Opérations arithmétiques
    int d = a + b;
    int e = b - a;
    int f = a * b;
    int g = b / a;
    int h = b % a;

    // Expressions booléennes
    boolean x = a < b;
    boolean y = b >= a;
    boolean z = (a + 1) == b;

}
