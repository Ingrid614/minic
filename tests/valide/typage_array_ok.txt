void main() {

    // Déclaration et initialisation de tableaux
    int tab[] = new int[5];

    // Accès et affectations
    tab[0] = 10;
    tab[1] = 20;
    tab[2] = 30;
    tab[3] = 40;
    tab[4] = 50;

    // Utilisation des valeurs du tableau
    int somme = tab[0] + tab[1] + tab[2] + tab[3] + tab[4];

    // Tableaux de float
    float tabFloat[] = new float[2];
    tabFloat[0] = 1.5;
    tabFloat[1] = 2.5;

    // Tableaux de booléens
    boolean tabBool[] = new boolean[2];
    tabBool[0] = true;
    tabBool[1] = false;
}